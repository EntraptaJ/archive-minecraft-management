query searchMods($search: String!) {
  searchMods(search: $search) {
    name
    id
    summary
    websiteUrl
    authors {
      name
    }
    latestFiles {
      fileName
      downloadUrl
      gameVersion
    }
  }
}
